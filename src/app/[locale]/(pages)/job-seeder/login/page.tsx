'use client';

import LoginPage from "@/components/Login/page";
import {Link} from "@/navigation";
import {Button} from "@mui/material";
import styles from "@/app/[locale]/(pages)/switch/index.module.scss";

export default function Login()
{
    return (
        <div>
            <LoginPage>
            </LoginPage>
        </div>
    );
}